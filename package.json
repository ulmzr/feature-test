{
  "name": "malina-fullstack-app",
  "version": "0.0.1",
  "description": "malina client and Derver server app, bundeled with esduild",
  "main": "index.js",
  "scripts": {
    "build": "node app",
    "dev": "node app --dev",
    "start": "node dist"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.12.9",
    "malinajs": "^0.6.30",
    "node-watch": "^0.7.1"
  },
  "dependencies": {
    "derver": "^0.4.16",
    "dotenv": "^10.0.0"
  }
}
